export function makePet(name, type, age, food, toys) {
    const pet = {
        name,
        type,
        age,
        food,
        toys,
    };
    return pet;
}

export function formatWork(book) {

}

